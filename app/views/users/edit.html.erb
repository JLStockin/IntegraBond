<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
	<%= render 'fields', :f =>f %><br />
	<fieldset>
		<div class="actions">
			<%= f.submit "Update" %>
			<%= f.submit "Cancel", :name => "cancel_button" %>
		</div>
	</fieldset>
<% end %>

